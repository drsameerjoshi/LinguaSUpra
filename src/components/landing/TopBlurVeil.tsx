/**
 * TopBlurVeil - a thin fixed strip at the top of the viewport that blurs
 * whatever page content sits behind it (Huzan-style). Purely presentational.
 */
export function TopBlurVeil() {
  return <div className="top-blur-veil" aria-hidden />;
}
