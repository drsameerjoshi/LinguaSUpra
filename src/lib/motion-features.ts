// Loaded on demand by <LazyMotion> so the animation feature set is a separate
// chunk instead of part of the initial bundle.
import { domAnimation } from "framer-motion";
export default domAnimation;
